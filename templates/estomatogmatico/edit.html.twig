{% extends 'base.html.twig' %}

{% block title %}Edit Estomatogmatico{% endblock %}

{% block body %}
 <div class="card-body p-5 bg-white rounded">
    <h6>Editar examen estomatogmático de: <b> {{paciente.pnombre}} {{paciente.snombre}} {{paciente.papellido}} {{paciente.sapellido}}</b></h6>
{% for flash_message in app.session.flashbag.get('exito') %}
    <div class="alert alert-success" role="alert">
       {{ flash_message }}
</div>
{% endfor %}
    {{ include('estomatogmatico/_form.html.twig', {'button_label': 'Actualizar'}) }}

     
    </div>
    <div>
    <a href="{{ path('app_estomatogmatico_new', {id: paciente.id, c:consulta.id}) }}">
     <button class="btn btn-block btn-outline-warning btn-sm">
<i class="fas fa-chevron-left">Regresar a la ficha médica</i>
</button>
      </a>
      </div>
{% endblock %}
