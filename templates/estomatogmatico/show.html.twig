{% extends 'base.html.twig' %}

{% block title %}Estomatogmatico{% endblock %}

{% block body %}
    <h1>Estomatogmatico</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estomatogmatico.id }}</td>
            </tr>
            <tr>
                <th>Opciones</th>
                <td>{{ estomatogmatico.opciones ? estomatogmatico.opciones|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ estomatogmatico.observaciones }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_estomatogmatico_index') }}">back to list</a>

    <a href="{{ path('app_estomatogmatico_edit', {'id': estomatogmatico.id}) }}">edit</a>

    {{ include('estomatogmatico/_delete_form.html.twig') }}
{% endblock %}
