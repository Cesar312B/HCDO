{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
{% set var = "Adminstrador" %}  
{% set var2 = "Enable" %}  
{% set var3 =  modulo.Profesion|default('undefined') %}  



{% if var3 != var  %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <h1>Registrar Administrador</h1>
        {# Mostrar mensajes de error #}
{% for flashMessage in app.flashes('error') %}
    <div class="alert alert-danger">
        {{ flashMessage }}
    </div>
{% endfor %}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.Nombre) }}
        {{ form_row(registrationForm.Apellido) }}
        {{ form_row(registrationForm.Cedula) }}
        {{ form_row(registrationForm.unidadesoperativas) }}
        {{ form_row(registrationForm.Fecha_Ingreso) }}
        
     

        <button type="submit" class="btn btn-primary">Registrar</button>
    {{ form_end(registrationForm) }}
    <a href="{{ path('app_login') }}" class="link-info">Login</a>
  {% else  %}
<h4>Ya existe un usuario Administeador </h4>
      {% endif %}
{% endblock %}