<form method="post" action="{{ path('app_diagnostico_d_delete', {'id': diagnostico_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diagnostico_d.id) }}">
    <button class="btn">Eliminar</button>
</form>
