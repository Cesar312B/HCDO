{% extends 'base.html.twig' %}

{% block title %}DiagnosticoD{% endblock %}

{% block body %}
    <h1>DiagnosticoD</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diagnostico_d.id }}</td>
            </tr>
            <tr>
                <th>Lado</th>
                <td>{{ diagnostico_d.lado }}</td>
            </tr>
            <tr>
                <th>Simbologia</th>
                <td>{{ diagnostico_d.simbologia }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ diagnostico_d.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_diagnostico_d_index') }}">back to list</a>

    <a href="{{ path('app_diagnostico_d_edit', {'id': diagnostico_d.id}) }}">edit</a>

    {{ include('diagnostico_d/_delete_form.html.twig') }}
{% endblock %}
